
<%= link_to 'Cadastrar categoria', new_product_category_path %>

<% @categories.each do |category| %>
  <%= link_to category.name, product_category_path(category) %>
  <%= category.description %>
  <br>
<% end %>
